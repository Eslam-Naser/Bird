
```bash
apt-get install libvirt-dev
apt-get install python3-libvirt 
 
apt install python3-pip
pip3 install libvirt-python


# Install guestfs

#yum install libguestfs-devel
apt-get install libguestfs-dev
pip3 install http://download.libguestfs.org/python/guestfs-1.40.2.tar.gz

#apt-get install python-guestfs
# yum search guestfs
```